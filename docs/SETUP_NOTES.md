📝 EpicPass Local Development Setup GuideThis guide reflects the exact, customized architecture built for your Laravel 13, Inertia.js (Vue 3), and Pest PHP ticketing application running via WSL2 (Ubuntu) and Docker Desktop.🛠️ The Environment OverviewUnlike basic configurations, this project runs inside a high-performance, containerized environment that perfectly mirrors real-world cloud production systems:Operating System Layer: Windows 11 / 10 hosting an isolated Ubuntu Linux Subsystem (WSL2).Code Storage Path: C:\laragon\www\laravel13\epicpass (Mounted seamlessly into Linux as /mnt/c/laragon/www/laravel13/epicpass).Containerized Runtime Engine: Laravel Sail (Docker Desktop) orchestrating an isolated PHP 8.4 app instance and a dedicated PostgreSQL (pgsql) database container.🚀 Daily Routine: How to Start and Stop the ProjectWhenever you turn on your computer or start a new coding session, execute these steps to boot up your entire project stack:1. Boot up the Backend ContainersOpen a standard Windows terminal (PowerShell or Command Prompt) and move into your Linux distribution environment:powershell# Step 1: Step into your real Linux OS
wsl

# Step 2: Navigate directly to your workspace folder
cd /mnt/c/laragon/www/laravel13/epicpass

# Step 3: Boot your infrastructure silently in the background
./vendor/bin/sail up -d
Use code with caution.2. Start the Frontend Dev ServerTo make sure your Vue 3, Inertia.js, and Tailwind CSS files compile live as you modify code, open a second terminal tab / window, type wsl to enter Linux, and start the asset watcher:bashcd /mnt/c/laragon/www/laravel13/epicpass
npm run dev
Use code with caution.🌐 Accessing Your ApplicationOnce both servers are running, open your favorite web browser and head directly to:👉 http://localhost🛑 Turning Off the ContainersWhen you are completely finished coding for the day and want to free up your computer's RAM, run this command inside your primary Linux terminal:bash./vendor/bin/sail down
Use code with caution.💾 Essential Commands ReferenceSince you are running inside a containerized setup via Laravel Sail, you must always prefix standard artisan or test directives with ./vendor/bin/sail. Here are your most essential tools:Database & Schema CommandsRun fresh database migrations:./vendor/bin/sail artisan migrateWipe database and re-run all schemas from scratch:./vendor/bin/sail artisan migrate:freshCreate a brand new migration file:./vendor/bin/sail artisan make:migration custom_file_nameTesting and Linting Quality EnginesExecute your modern Pest PHP feature tests:./vendor/bin/sail artisan testRun tests in rapid parallel speed loops:./vendor/bin/sail test --parallelEnforce clean code styling automatically using Laravel Pint:./vendor/bin/sail pint🔑 Crucial Gotchas & Fixes for Windows UsersKeep these handy in case your terminal environment throws a common Windows virtualization issue:Error: Docker or Podman is not running.Fix: Ensure the Docker Desktop desktop app is fully running and showing a green light. If it is already running, open a standard Windows PowerShell window (outside of Linux) and type wsl --shutdown to force-sync the system communication socket layers.Error: docker-desktop:/tmp/...# prompt appearsFix: You accidentally fell inside Docker's hidden utility box instead of your workspace. Type exit to return to Windows, then launch explicitly into your true environment using wsl -d Ubuntu.